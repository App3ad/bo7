<Alloy>
  <Collection src="product"/>
  <Window>
    <ListView defaultItemTemplate="template" id="list" method="createCollectionView" module="CollectionView" onItemclick="productListClicked">
      <Templates>
        <ItemTemplate name="template">
          <View class="container ccBox">
            <Label bindId="title" class="titleLbl"/>
            <Label bindId="time" class="timeLbl"/>
            <View class="lineView">
              <Label bindId="type" class="iconLbl"/>
              <Label bindId="bank" class="iconLbl"/>
              <Label bindId="period" class="iconLbl"/>
            </View>
          </View>
        </ItemTemplate>
        <ItemTemplate name="add">
          <Label class="addBtn" bindId="addBtn"/>
        </ItemTemplate>
      </Templates>
      <ListSection dataCollection="product" dataTransform="transfomer" method="createCollectionSection" module="de.marcelpociot.collectionview">
        <ListItem bank:backgroundColor="{bankColor}" bank:text="{bankCode}" lastPaymentDate="{lastPaymentDate}" method="createCollectionItem" module="de.marcelpociot.collectionview" myId="{id}" period:text="{period}" periodNumber="{periodNumber}"
          periodType="{periodType}" time:text="{time}" title:text="{title}" type:backgroundColor="{typeColor}" type:text="{typeCode}"/>
      </ListSection>
      <ListSection method="createCollectionSection" module="de.marcelpociot.collectionview">
        <ListItem method="createCollectionItem" module="de.marcelpociot.collectionview" template="add"/>
      </ListSection>
    </ListView>
  </Window>
</Alloy>
